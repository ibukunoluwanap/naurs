from django.urls import path
from . import views

urlpatterns = [
    # path('', views.Instructor.as_view(), name='instructor_page'),
    # path('<int:pk>/', views.InstructorDetail.as_view(), name='instructor_detail_page'),
]